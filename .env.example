# Copy to .env (and Cloudflare project secrets) and fill in.
# Phase 9 — live RAG demo only. Not needed for the static build.

# LLM provider key (vendor TBD — see ULTRAPLAN §15 #8)
# LLM_API_KEY=

# Cloudflare Turnstile (bot gate for the public /api/chat demo)
# TURNSTILE_SECRET_KEY=
# PUBLIC_TURNSTILE_SITE_KEY=

# Vector store (if using Supabase + pgvector instead of Vectorize)
# SUPABASE_URL=
# SUPABASE_SERVICE_KEY=
